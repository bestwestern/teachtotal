# teachtotal

##preflopdisp

Kopier pl filen og billederne fra child public imgs til preflopdisp public imgs (efter at have valgt preflop)
Build og vercel build mappen prflodisp

quiz-delen: run buildpreflop cd prflo vercel --prod

de forskellige program-samlinger findes i databasejson filen
